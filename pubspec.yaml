name: nasa_api
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  card_swiper: ^3.0.1
  flutter:
    sdk: flutter
  http: ^1.2.2
  path_provider: ^2.1.5
  provider: ^6.1.2
  url_launcher: ^6.3.1
  webview_flutter: ^4.0.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/no-image.jpg
    - assets/loading.gif
    - assets/noImage.png
